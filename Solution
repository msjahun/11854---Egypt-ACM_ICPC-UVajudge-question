#include<iostream>
#include<cmath>
using namespace std;
int main(){
double a,b,c;

for ( ;1; ){
cin >> a >> b >> c;
if(a==0 || b==0 || c==0){
	break;}
else {
a= pow(a,2);
b= pow(b,2);
c= pow(c,2);
	if(a + b == c)
		cout << "right"<< endl;
			else 
				cout << "wrong" << endl;}
}
return 0;
}
